services:
  postgres:
    image: postgres:16.3
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}

  worker:
    image: docker:27.0.3
    volumes:
      - ./workspace:/workspace
